language: go

go:
  - 1.9
  - tip

go_import_path: gopkg.in/src-d/go-mysql-server.v0

matrix:
  fast_finish: true
  allow_failures:
    - go: tip

sudo: required

install:
  - make dependencies

script:
  - make test-coverage
