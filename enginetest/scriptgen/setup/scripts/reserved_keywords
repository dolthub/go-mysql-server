exec
CREATE TABLE `reservedWordsTable` (
  `Timestamp` varchar(20) NOT NULL,
  `and` text,
  `or` text,
  `select` text,
  PRIMARY KEY (`Timestamp`)
)
----

exec
insert into reservedWordsTable values
    ('1', '1.1', 'aaa', 'create')
----
